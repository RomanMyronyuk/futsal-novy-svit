# ⚽ Futsal League Management App

Додаток для управління футзальною лігою з можливістю відстеження матчів, статистики та результатів.

## Особливості

- 📊 **Турнірна таблиця** - автоматичний підрахунок очок, голів та місць команд
- 🎮 **Управління ігровими днями** - створення та редагування ігрових днів
- ⚽ **Реєстрація матчів** - додавання та редагування результатів матчів
- 👥 **Управління гравцями** - додавання гравців та відстеження їх голів
- 🔄 **Трансфери** - можливість переміщення гравців між командами
- 📈 **Статистика гравців** - перегляд статистики по голах та іграх за весь час
- 🔥 **Firebase Firestore** - хмарна база даних з синхронізацією в реальному часі
- 💾 **Fallback на LocalStorage** - працює офлайн якщо Firebase не налаштовано

## Технології

- React 18
- TypeScript
- Firebase Firestore
- React Router
- Styled Components
- Vite

## Встановлення та запуск

### 1. Встановіть залежності:

```bash
npm install
```

### 2. Налаштуйте Firebase (опціонально):

Див. розділ "Налаштування Firebase" нижче.

### 3. Запустіть dev сервер:

```bash
npm run dev
```

Додаток буде доступний за адресою: http://localhost:5173/futsal-league/

## 🔥 Налаштування Firebase

Для збереження даних у хмарі та синхронізації між пристроями:

### Крок 1: Створіть проект Firebase

1. Перейдіть на [Firebase Console](https://console.firebase.google.com/)
2. Натисніть "Add project" або "Створити проект"
3. Введіть назву проекту (наприклад, "futsal-league")
4. Вимкніть Google Analytics (не потрібен) або залиште
5. Натисніть "Create project"

### Крок 2: Створіть веб-додаток

1. На головній сторінці проекту натисніть іконку "</>" (Web)
2. Введіть назву додатку (наприклад, "Futsal League App")
3. Не вмикайте Firebase Hosting (ми використовуємо GitHub Pages)
4. Натисніть "Register app"
5. Скопіюйте конфігурацію - вам знадобляться:
   - `apiKey`
   - `authDomain`
   - `projectId`
   - `storageBucket`
   - `messagingSenderId`
   - `appId`

### Крок 3: Створіть Firestore Database

1. У бічному меню виберіть "Build" → "Firestore Database"
2. Натисніть "Create database"
3. Виберіть режим:
   - **Production mode** - для реального використання
   - **Test mode** - для тестування (дані доступні всім 30 днів)
4. Виберіть локацію сервера (europe-west1 для Європи)
5. Натисніть "Enable"

### Крок 4: Налаштуйте правила безпеки

Для тестового режиму (відкритий доступ):

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

### Крок 5: Додайте конфігурацію в проект

Створіть файл `.env.local` в корні проекту:

```env
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
```

Або відредагуйте `src/config/firebase.ts` напряму:

```typescript
const firebaseConfig = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID',
};
```

### Крок 6: Перезапустіть додаток

```bash
npm run dev
```

У правому нижньому куті має з'явитися зелений індикатор "Firebase" замість помаранчевого "Локально".

## Деплой на GitHub Pages

### 1. Оновіть базовий шлях в `vite.config.ts`:

```typescript
base: '/your-repo-name/',
```

### 2. Для GitHub Pages з Firebase:

Додайте змінні середовища в GitHub:
1. Settings → Secrets and variables → Actions
2. Додайте кожну змінну VITE_FIREBASE_* як secret

### 3. Створіть репозиторій та задеплойте:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git push -u origin main

npm run deploy
```

Додаток буде доступний за адресою: `https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/`

## Правила підрахунку очок

- Перемога: 3 очки
- Нічия: 1 очко
- Поразка: 0 очок

Сортування команд:
1. За кількістю очок
2. За різницею забитих/пропущених голів
3. За кількістю забитих голів

## Структура проекту

```
src/
├── components/       # React компоненти
│   ├── Navigation.tsx
│   └── UI.tsx       # Базові UI компоненти
├── config/          # Конфігурація
│   └── firebase.ts  # Firebase конфігурація
├── context/         # React Context для управління даними
│   └── AppContext.tsx
├── pages/           # Сторінки додатку
│   ├── GameDaysList.tsx
│   ├── CreateGameDay.tsx
│   ├── GameDayView.tsx
│   ├── PlayersPage.tsx
│   ├── TeamsPage.tsx
│   └── PlayerStatsPage.tsx
├── styles/          # Стилі та тема
│   └── theme.ts
├── types.ts         # TypeScript типи
├── App.tsx          # Головний компонент
└── main.tsx         # Entry point
```

## Firestore структура даних

```
firestore/
├── teams/           # Колекція команд
│   └── {teamId}/
│       ├── name: string
│       └── color: string
├── players/         # Колекція гравців
│   └── {playerId}/
│       └── name: string
└── gameDays/        # Колекція ігрових днів
    └── {gameDayId}/
        ├── name: string
        ├── date: string
        ├── isActive: boolean
        ├── playerTeamAssignments: array
        └── matches: array
```

## Ліцензія

MIT

## Автор

Створено для управління футзальною лігою 🎯⚽
